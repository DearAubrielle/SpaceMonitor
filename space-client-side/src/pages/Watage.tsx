import Users from "./Users";

const Watage = () => {
    return (
        <div>
            <Users />
            {/* Add any additional components or content here */}
            {/* Example: <ImageUpload onImageUpload={(imageData) => console.log(imageData)} /> */}
            {/* Example: <Dnd /> */}
            <h1>Watage Page</h1>
            <p>Welcome to the Watage page. This is where you can monitor and manage your space usage.</p>
        </div>
    );
};

export default Watage;